# CsDemoAnalyzer default configuration
# Copy this file and customize for your use case.

parser:
  provider: null  # null = auto-detect (faceit/valve/perfect_world/...)
  tick_rate: 64
  include_warmup: false

analysis:
  enabled_modules:
    - basic_stats
    - ratings

cache_dir: .cache
log_level: INFO
